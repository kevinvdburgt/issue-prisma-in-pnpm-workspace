{
  "name": "@issue-prisma-in-pnpm-workspace/sample",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only --rs ./src/main.ts",
    "build": "rimraf dist && prisma generate && tsc",
    "start": "node ./dist/main.js"
  },
  "dependencies": {
    "@prisma/client": "^3.6.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.1",
    "prisma": "^3.6.0",
    "rimraf": "^3.0.2",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.4"
  }
}
